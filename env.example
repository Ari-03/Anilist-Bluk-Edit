# AniList API Endpoint (optional, defaults to https://graphql.anilist.co)
ANILIST_API_URL=https://graphql.anilist.co

# Note: This application now uses personal access tokens instead of OAuth.
# Users will generate their own tokens through the AniList developer interface.
# No environment variables are required for authentication. 